apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud
spec:
  values:
    internalDatabase:
      enabled: false
    mariadb:
      enabled: false
    externalDatabase:
      enabled: true
      type: mysql
      # Sans port : le service mariadb n'expose que 3306. Le 6446 tenait de
      # l'ancienne infrastructure MySQL, dont le routeur MaxScale ecoutait la ;
      # repris tel quel a la migration, il ne designait plus rien et faisait
      # echouer l'installation sur « SQLSTATE[HY000] [2002] Connection timed
      # out ». Le custom.config.php de ce meme fichier portait deja l'hote sans
      # port : les deux valeurs se contredisaient.
      host: mariadb.controller-database.svc.cluster.local
      database: fdj-nextcloud
      existingSecret:
        enabled: true
        secretName: nextcloud
        usernameKey: mariadb-user
        passwordKey: mariadb-password
    persistence:
      enabled: true
      storageClass: ${storage_class}
      accessMode: ReadWriteOnce
      size: 30Gi
    nextcloud:
      host: nextcloud.${domain}
      datadir: /var/www/html/data
      # Important : trusted domains
      configs:
        custom.config.php: |-
          <?php
          $CONFIG = array(
            'trusted_domains' => [
              'nextcloud.${domain}',
              'cloud.${domain}'
            ],
            'overwrite.cli.url' => 'https://nextcloud.${domain}',
            'overwriteprotocol' => 'https',
            'dbhost' => 'mariadb.controller-database.svc.cluster.local',
          );
      existingSecret:
        enabled: true
        secretName: nextcloud
        usernameKey: nextcloud-user
        passwordKey: nextcloud-password
    ingress:
      enabled: false