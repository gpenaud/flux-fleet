
apiVersion: k8up.io/v1
kind: PodConfig
metadata:
  name: podconfig
spec:
  template:
    spec:
      securityContext:
        fsGroup: 1001
      containers:
        - name: backup
          securityContext:
            runAsUser: 1001
            runAsGroup: 1001