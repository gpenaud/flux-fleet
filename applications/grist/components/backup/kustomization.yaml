
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pod-config.yaml
  - pre-backup-pod.yaml
  - ../../../shared/components/backup/schedule.yaml

patches:
  - target:
      kind: PersistentVolumeClaim
    patch: |-
      - op: add
        path: /metadata/annotations/k8up.io~1backup
        value: "true"