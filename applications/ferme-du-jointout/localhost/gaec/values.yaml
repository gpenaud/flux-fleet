apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: le-portail-gaec
spec:
  values:
    wordpressUsername: ""
    wordpressPassword: ""
    wordpressEmail: dugrainamoudre@gmail.com
    mariadb:
      enabled: true
      auth:
        existingSecret: gaec-mariadb
    externalDatabase:
      host: wordpress-mariadb-0.le-portail-gaec.svc.cluster.local
      port: 3306
      user: le-portail
      database: le-portail
      existingSecret: gaec-wordpress
      existingSecretPasswordKey: WORDPRESS_DATABASE_PASSWORD
    wordpressPlugins: []
    service:
      type: ClusterIP
    ingress:
      enabled: true
      ingressClassName: traefik
      hostname: le-portail.gpenaud.localhost
      annotations:
        kubernetes.io/ingress.class: traefik
        cert-manager.io/cluster-issuer: mkcert-issuer
      extraTls:
        - hosts:
            - le-portail.gpenaud.localhost
          secretName: le-portail.gpenaud.localhost-tls
  valuesFrom:
    - kind: Secret
      name: gaec
      valuesKey: WORDPRESS_USERNAME
      targetPath: wordpressUsername
    - kind: Secret
      name: gaec
      valuesKey: WORDPRESS_PASSWORD
      targetPath: wordpressPassword
