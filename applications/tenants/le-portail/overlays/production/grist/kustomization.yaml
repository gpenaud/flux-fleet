
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ${tenant}-grist

resources:
  - ../../../../../catalog/grist
  - ../../../../../catalog/grist/components/backup
  - storage.yaml
  - ingressroute.yaml
  - secrets.enc.yaml

patches:
  - target:
      kind: HelmRelease
    path: values.yaml