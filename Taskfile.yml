version: '3'

includes:
  cluster:
    taskfile: ./tasks/cluster.yml
    # dir: ./tasks
    # optional: true

  backup:
    taskfile: ./tasks/backup.yml
    # dir: ./tasks
    # optional: true

  reconcile:
    taskfile: ./tasks/reconcile.yml
    # dir: ./tasks
    # optional: true