version: '3'

includes:
  # global scope
  infrastructure: ./tools/tasks/infrastructure.yml
  package: ./tools/tasks/package.yml

  # cluster scope
  infra01:
    taskfile: ./tools/tasks/cluster.yml
    vars: { CLUSTER: infra01, VPN_DOMAIN: vpn.infra01.alter-it.eu }
  infra02:
    taskfile: ./tools/tasks/cluster.yml
    vars: { CLUSTER: infra02, VPN_DOMAIN: vpn.staging.infra02.alter-it.eu }