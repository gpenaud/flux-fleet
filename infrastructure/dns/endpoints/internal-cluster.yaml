
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: internal-cluster
  namespace: controller-dns
spec:
  endpoints:
    - dnsName: internal.${domain_prefix}${project}.${domain}
      recordType: A
      targets:
        - ${internal_service_ip}
      recordTTL: 300