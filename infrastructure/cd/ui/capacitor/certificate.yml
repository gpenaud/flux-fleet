---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: capacitor
  namespace: flux-system
spec:
  secretName: capacitor-cert
  dnsNames:
    - "${capacitor_fqdn}"
  issuerRef:
    name: mkcert-issuer
    kind: ClusterIssuer