---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard.${domain_prefix}${project}.${domain}
  namespace: controller-certificate
spec:
  secretName: wildcard.${domain_prefix}${project}.${domain}.tls
  secretTemplate:
    annotations:
      reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
      reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: "controller-.*"
      reflector.v1.k8s.emberstack.com/reflection-auto-enabled: "true"
      reflector.v1.k8s.emberstack.com/reflection-auto-namespaces: "controller-.*"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "${domain_prefix}${project}.${domain}"
  dnsNames:
    - "${domain_prefix}${project}.${domain}"
    - "*.${domain_prefix}${project}.${domain}"
    - "*.internal.${domain_prefix}${project}.${domain}"