apiVersion: v1
kind: Namespace
metadata:
  name: controller-capsule
  labels:
    alter-it.org/controller: capsule
    alter-it.org/managed-by: fluxcd

---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: projectcapsule
  namespace: controller-capsule
spec:
  interval: 24h
  url: https://projectcapsule.github.io/charts
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: capsule
  namespace: controller-capsule
spec:
  interval: 30m
  chart:
    spec:
      chart: capsule
      version: "0.7.*"
      sourceRef:
        kind: HelmRepository
        name: projectcapsule
        namespace: controller-capsule
      interval: 12h
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
  values:
    manager:
      options:
        forceTenantPrefix: true
        capsuleUserGroups:
          - capsule.clastix.io
    replicaCount: 2