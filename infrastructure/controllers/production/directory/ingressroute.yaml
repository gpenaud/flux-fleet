apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lldap-ui
  namespace: controller-directory
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`directory.alter-it.eu`)
      kind: Rule
      services:
        - name: lldap
          port: 17170
  tls:
    secretName: directory-alter-it-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: directory-alter-it-tls
  namespace: controller-directory
spec:
  secretName: directory-alter-it-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - directory.alter-it.eu