apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - storage

components: []
  # required for most of the controllers (cert-manager, external-dns, etc.)
  # - ../../../base/certificates/observability