---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: monitoring.internal.${domain_prefix}${project}.${domain}
  namespace: controller-observability
spec:
  secretName: monitoring.internal.${domain_prefix}${project}.${domain}.tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "monitoring.internal.${domain_prefix}${project}.${domain}"
  dnsNames:
    - "monitoring.internal.${domain_prefix}${project}.${domain}"