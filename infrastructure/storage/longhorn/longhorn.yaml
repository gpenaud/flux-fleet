
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: local-path
  namespace: controller-storage
spec:
  interval: 24h
  url: https://charts.local-path.io

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: local-path
  namespace: controller-storage
spec:
  interval: 30m
  chart:
    spec:
      chart: local-path
      version: "v1.x"
      sourceRef:
        kind: HelmRepository
        name: local-path
        namespace: controller-storage
  values:
    defaultSettings:
      defaultDataPath: /var/lib/local-path
      backupTarget: ""
      snapshotDataIntegrity: disabled
      autoCleanupSystemGeneratedSnapshot: true
      disableScheduledBackup: true
    persistence:
      defaultClass: true
      defaultClassReplicaCount: 1
      defaultFsType: xfs      
    ingress:
      enabled: false