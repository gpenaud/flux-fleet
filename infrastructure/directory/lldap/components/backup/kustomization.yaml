apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pod-config.yaml
  - pre-backup-pod.yaml
  # Schedule dédié et non mutualisé : voir l'explication en tête de schedule.yaml
  # (podConfigRef doit être posé par job, ce que le Schedule partagé ne fait pas).
  - schedule.yaml
