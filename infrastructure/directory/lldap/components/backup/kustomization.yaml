apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pod-config.yaml
  - pre-backup-pod.yaml
  # Schedule mutualisé : même politique de rétention que les applications de
  # tenants (4 derniers, 4 hebdomadaires, 2 mensuels, 1 annuel), définie en un
  # seul endroit. Il attend ${application} et ${tenant}, fournis par sync.yaml.
  - ../../../../../applications/shared/components/backup/schedule.yaml
