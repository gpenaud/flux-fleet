---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-dashboard
  namespace: kube-system
spec:
  secretName: traefik-dashboard-cert
  dnsNames:
    - "traefik.gpenaud.localhost"
  issuerRef:
    name: mkcert-issuer
    kind: ClusterIssuer