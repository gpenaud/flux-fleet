
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tenants
  namespace: flux-system
spec:
  dependsOn:
    # - name: infrastructure
    - name: infrastructure-controller-storage
    - name: infrastructure-controller-ingress-traefik
    - name: infrastructure-controller-database
    - name: infrastructure-controller-certificate
    - name: infrastructure-controller-authentication
    - name: infrastructure-controller-reflector
    - name: infrastructure-controller-tenant
    - name: infrastructure-controller-workflow
  interval: 1h
  retryInterval: 2m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./clusters/infra02/tenants
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age-private-key
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars
        optional: false