
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # rbac
  - rbac/serviceaccount.yaml
  - rbac/rolebinding.yaml
  - rbac/role.yaml
  # steps
  - steps/create-k8up-restore.clusterworkflowtemplate.yaml
  - steps/resolve-snapshot-id.clusterworkflowtemplate.yaml
  - steps/scale.clusterworkflowtemplate.yaml
  - steps/wait-for-k8up-restore.clusterworkflowtemplate.yaml